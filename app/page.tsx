export default function Home() {
  return (
    <main id="main-content" tabIndex={-1}>
      <section className="shell-placeholder" aria-labelledby="shell-title">
        <p className="eyebrow">Portfolio foundation</p>
        <h1 id="shell-title">Application shell ready.</h1>
        <p>
          The portfolio experience is being assembled in focused, production-ready
          increments.
        </p>
      </section>
    </main>
  );
}
